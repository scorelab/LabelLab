const intialState ={
	isRegistering:false,
	error:false,
	err_field:"",
	statusText:""
}

const register = (state = intialState , action )=>{
	switch(action.type){
	default:
		return state
	}
}

export default register